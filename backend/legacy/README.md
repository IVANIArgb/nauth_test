# Устаревший код (M5)

Модули `real_kerberos_auth.py` и `simplified_real_kerberos_auth.py` **не подключаются** к приложению.

Актуальная аутентификация: `auth/new_auth.py`, `auth/auth_script.py`.

Не импортируйте файлы из этой папки в новом коде.
